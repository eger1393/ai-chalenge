# Deploy Report

Дата: 2026-04-11 16:39
Статус: Done

## Задача

Выполнить деплой проекта `first-lesson` на сервер `167.235.226.104:2222`.

## Что проверено локально

- `backend`: `npx tsc --noEmit -p tsconfig.build.json` - успешно
- `frontend`: `npx tsc --noEmit` - успешно

## Что задеплоено

- Ветка: `develop`
- Коммит: `780da18`
- Состояние origin: `develop` уже совпадал с локальным `HEAD`
- Незакоммиченные локальные изменения относятся только к служебным markdown-файлам и не деплоились

## Что выполнено на сервере

- Запущен `bash /srv/ai-chalange/update.sh first-lesson`
- `git pull --ff-only`: `Already up to date`
- Выполнен пересбор Docker-образов и перезапуск `docker compose`

## Validation

- `docker compose ps`: все 5 контейнеров в статусе `Up`, PostgreSQL - `healthy`
- `docker compose logs --tail=20 backend`: backend стартовал успешно
- Backend подключился к PostgreSQL
- Backend подключился к MCP `postgres` и `github-explorer`

## Замечания

- Полный `frontend` build в Docker занимает заметное время; локально для быстрой преддеплойной проверки использована TypeScript-компиляция без bundling
- В `PROJECT_MAP.md` есть ссылка на `deploy.md`, но файла в репозитории нет
