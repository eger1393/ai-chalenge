давай перепланируем сущности для работы с чатом. 

давай переименуем Task в проект. 
проект - это некий аггрегат над групой диалогов. 
у него есть какая-то цель (рабочая память туда пишем инфу о проекте в целом)
у него могут быть инванианты. 

все диалоги создаются только в рамках проекта. 


диалог - хранит список сообщений, хранит параметры диалога (выбранная модель, лимиты контекста, систем промпт и тд)

Давай сделаем контекст - отдельной сущностью. 
контекст всегда привязан к диалогу (1 к 1)
контектс хранит текущую стратегию саммаризации. 
и информацию в рамках стратегии (мб сделаем это JSONB полем, а на уровне кода, для каждой стратегии свою опишем модель)

дальше сущность - сообщение - это связка запрос от пользователя - ответ от нейрокни. 
сообщение всегда привязано к диалогу. 
учти работа над сообщением подразумевает некий переход по состояниям (сейчас из 3 но давай архитектурно заложем возможность расширения). 

отдельно я хочу что-бы ты создал доп сущности зависящие от сообещения, например снапшот настроек диалога в момент сообщения, информация о расходах, контекст сообщения (который улетел в нейронку или улетал на каждом из этапов) и решил стоит ли их жестко типизировать (отдельные таблицы в БД или сделать через JSONB, но вариант с типизацией мне нравится, т.к. контракты будут более очевидны)

в этой итерации я хочу определится с тем как у нас будут выглядеть данные в БД. 
так-же учти что мы находимся в ЗБТ, поэтому можешь не парится с миграцией данных и просто грохнуть все данные. 




-- ПОЛУЧИЛ СПЕКУ ПО БД

Хочу заполнить контекст инфой о архитектуре модулей\сервисов
-> 
отлично, теперь изучи как устроена архитектура бизнес логика на        
  бэкенде (модули, сервисы, какие у них зоны ответсвенности и тд) 

-- ПОЛУЧИЛ 2026-04-05-backend-logic-analysis

-> 
1) давай сделаем хелпер для обертывания нескольких запросов в транзакцию. 
я хочу что-бы можно было написать что-то вроде 
await transactionHelper.run(() => {
    await userRepository.update(...)
    await chatRepository.update(...)
})
при этом давай не прокидывать контекст транзакции руками в репозитории, давай хранить его где-то в database модуле и пусть там будет логика, которую будут дергать все репозитории и либо репозиторий получает контекст транзакции и выполняет запрос в нем, или он получает контекст соединения, если вызван вне транзакции. 

давай сделаем отдельный модуль, который будет отвечать за контекст и имплементировать различные стратегии по работе с контекстом (саммаризация, факты, ветки) которыу уже есть. 

так-же я хочу в отдельный модуль вынести процессинг сообщения (переход по стадиям - планирование, выполнения и тд) сюда-же унести проверки на промпт инжектинг и тд. 

какие еще модули стоит сделать и как лучше разбить приложение на части? 


----