Прочитай $ARGUMENTS и подробно проинтервьюируй меня, используя AskUserQuestion, буквально обо всём: техническая реализация, UI и UX, риски/опасения, компромиссы (tradeoffs) и т. д. — но убедись, что вопросы не очевидные.

Погружайся максимально глубоко и продолжай интервью непрерывно, пока оно не будет полностью завершено, а затем запиши спецификацию в файл `./swarm-report/<feature>/<feature>-spec.md`, твоя задача не описывать решение проблемы \ задачи, а подробно сформулировать постановноку задачи, риски, ДоД-ы и эджкейсы. 
Выработка решения и его имплементация будет сделана отдельно, оставь выработку решения другому агенту.

если на вход тебе был задан файл из которого ты читал постановку, перенеси этот файл так-же в папку `./swarm-report/<feature>` и назови его `<feature>_input.md`. 
если постановка была задана в виде текста, то просто запиши его в файл `<feature>_input.md`