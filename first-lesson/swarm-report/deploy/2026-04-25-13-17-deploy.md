# Отчёт: deploy

## Задача

Задеплоить на stage ограничение `3` одновременных вызовов нейросетей в backend.

## Что задеплоено

- Ветка: `develop`
- Коммит: `63fbd6b feat(ai): limit concurrent llm requests`
- На сервере `first-lesson` обновлён до `63fbd6b`

## Что выполнено

- Локально выполнен преддеплойный `backend` build: `npm run build`
- Изменения закоммичены в `63fbd6b`
- Push выполнен в GitHub по SSH через `ssh.github.com:443`
- На сервере выполнен `bash /srv/ai-chalange/update.sh first-lesson`

## Validation

- `docker compose ps`: все 6 контейнеров подняты
- `postgres` в статусе `healthy`
- `postgres-mcp`, `github-explorer-mcp`, `knowledge-base-mcp` в статусе `healthy`
- `backend` и `frontend` подняты на `6500` и `6501`
- `docker compose logs --tail=20 backend`:
  - backend стартовал успешно
  - есть подключение к PostgreSQL
  - приложение слушает порт `3000`

## Ограничения и наблюдения

- В локальном WSL `origin` по-прежнему требует отдельную SSH-аутентификацию, поэтому push выполнен прямым SSH URL без изменения git-конфига репозитория
- Старый локальный файл `swarm-report/deploy/2026-04-24-13-52-deploy.md` оставлен вне коммита как несвязанный с этой задачей артефакт
- В backend-логах после старта снова есть предупреждения `MCP server "..." returned 0 tools`; они не блокируют запуск и уже наблюдались на stage ранее

## Статус

Готово
