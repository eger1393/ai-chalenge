# Отчёт: deploy

## Задача

Задеплоить на stage локальные изменения из `develop`.

## Что задеплоено

- Ветка: `develop`
- Коммит: `8442d3d feat(ai): harden local ollama pipeline`
- На сервере `first-lesson` обновлён до `8442d3d`

## Что выполнено

- Локально выполнены преддеплойные проверки:
  - `backend`: `npx tsc --noEmit -p tsconfig.build.json`
  - `frontend`: `npx tsc --noEmit`
- Локальные изменения закоммичены в `8442d3d`
- Push выполнен в GitHub по SSH через `ssh.github.com:443`
- На сервере выполнен `bash /srv/ai-chalange/update.sh first-lesson`

## Validation

- `docker compose ps`: все 6 контейнеров подняты
- `postgres` в статусе `healthy`
- `postgres-mcp`, `github-explorer-mcp`, `knowledge-base-mcp` в статусе `healthy`
- `backend` и `frontend` подняты на `6500` и `6501`
- `docker compose logs --tail=20 backend`:
  - backend стартовал успешно
  - есть подключение к PostgreSQL
  - приложение слушает порт `3000`

## Ограничения и наблюдения

- В локальном WSL `origin` настроен на HTTPS без доступных кредов, поэтому push выполнен прямым SSH URL без изменения git-конфига
- В backend-логах после старта есть предупреждения `MCP server "..." returned 0 tools`; это наблюдение уже встречалось в предыдущих stage-деплоях и не остановило запуск

## Статус

Готово
